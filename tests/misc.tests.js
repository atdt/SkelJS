
// -------
// tests
//

// Given a sufficiently large population, simple random sampling should
// generate sample sizes that hew closely to the specified sampling factor.
test( 'Dummy test', function () {
    var x = 4;
    ok( x / 2 === 2 );

} );
